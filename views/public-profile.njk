{% extends "layout.njk" %}
{% block content %}
<div class="dashboard-header auth-container" style="margin-top: 2rem; margin-bottom: 3rem; padding: 2rem;">
    <h1>Profil de {{ targetUser.username }}
    </h1>
    {% if targetUser.bio %}
        <p style="font-style: italic; color: #ccc;">"{{ targetUser.bio }}"</p>
    {% endif %}

            <!-- Friend feature removed per user request -->
</div>

<div class="lists-container">
    <section class="movie-section">
        <h2>📌 Watchlist de {{ targetUser.username }}
        </h2>
        <div class="movie-grid">
            {% if watchlist and watchlist.length > 0 %}
                {% for movie in watchlist %}
                    <div class="movie-card">
                        <a href="/movie/{{ movie.movie_id }}">
                            {% if movie.poster_path %}
                                <img src="https://image.tmdb.org/t/p/w500{{ movie.poster_path }}"
                                    alt="{{ movie.title }}">
                            {% else %}
                                <div class="no-poster">
                                    {{ movie.title }}
                                </div>
                            {% endif %}
                                <div class="movie-info">
                                    <h3>
                                        {{ movie.title }}
                                    </h3>
                                </div>
                        </a>
                    </div>
                {% endfor %}
            {% else %}
                <p>Aucun film dans sa Watchlist.</p>
            {% endif %}
        </div>
    </section>

    <section class="movie-section" style="margin-top: 4rem;">
        <h2>👁️ Films vus par {{ targetUser.username }}
        </h2>
        <div class="movie-grid">
            {% if watchedMovies and watchedMovies.length > 0 %}
                {% for movie in watchedMovies %}
                    <div class="movie-card">
                        <a href="/movie/{{ movie.movie_id }}">
                            {% if movie.poster_path %}
                                <img src="https://image.tmdb.org/t/p/w500{{ movie.poster_path }}"
                                    alt="{{ movie.title }}" style="opacity: 0.7;">
                            {% else %}
                                <div class="no-poster">
                                    {{ movie.title }}
                                </div>
                            {% endif %}
                                <div class="movie-info">
                                    <h3>
                                        {{ movie.title }}
                                    </h3>
                                </div>
                        </a>
                    </div>
                {% endfor %}
            {% else %}
                <p>Aucun film marqué comme vu.</p>
            {% endif %}
        </div>
    </section>
</div>
{% endblock %}